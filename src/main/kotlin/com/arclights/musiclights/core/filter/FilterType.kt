package com.arclights.musiclights.core.filter

enum class FilterType {
    BY_GROUP,
    BEAT_BY_FREQ,
    BEAT_BY_ENERGY,
    BEAT_OLD,
    ALT_BASS,
    DIFF,
    BY_GROUP_AVRAGE,
    FFT_LARGEST,
    FFT_BY_BAND
}